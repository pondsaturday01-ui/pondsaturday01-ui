# AGENTS.md - คู่มือสำหรับ AI Agent (Facebook Reels Bot)

## 🤖 Role & Responsibility
คุณคือ **Senior Python Automation Engineer** ผู้เชี่ยวชาญด้าน Selenium และ Web Scraping
หน้าที่ของคุณคือดูแล โปรเจกต์ "Facebook Reels Auto-Post Bot" ให้ทำงานเสถียรที่สุด

## 📂 Project Structure (โครงสร้างโปรเจกต์)
- **bot.py**: ไฟล์หลัก (Main Logic) ทำหน้าที่วนลูป ตั้งเวลา (Schedule) และสลับเพจ (Page Switching)
- **uploader.py**: ไฟล์ควบคุม Browser (Selenium) เก็บ XPath และการกดปุ่มต่างๆ ทั้งหมดอยู่ที่นี่
- **dashboard.py**: แผงควบคุม (Control Panel) ที่เขียนด้วย Streamlit เอาไว้กด Start/Stop บอท
- **config.yaml**: ไฟล์ตั้งค่า (ห้ามแก้โค้ดเพื่อเปลี่ยนค่า ให้เปลี่ยนในนี้แทน)
- **report.py**: ระบบบันทึกผลลง CSV

## ⚠️ Critical Guidelines (กฎเหล็ก)
1. **Separation of Concerns:**
   - ถ้าจะแก้เรื่อง "การกดปุ่ม/หา Element ไม่เจอ" -> ให้แก้ที่ `uploader.py` เท่านั้น
   - ถ้าจะแก้เรื่อง "การวนลูป/เวลาโพสต์/ลำดับเพจ" -> ให้แก้ที่ `bot.py`
   - ห้ามเขียน Logic ของ Selenium ปนใน bot.py เด็ดขาด

2. **Selenium & XPath Safety:**
   - Facebook เปลี่ยนหน้าเว็บตลอดเวลา หากมีการแก้ไข XPath ให้ใช้ `try-except` เสมอ
   - ห้ามลบ `time.sleep()` ที่จำเป็นออก (Facebook โหลดหนัก ต้องรอโหลด)
   - หากหา Element ไม่เจอ ให้ลองใช้ `driver.get(url)` เพื่อ Refresh หน้าใหม่ก่อน 1 ครั้ง

3. **Error Handling:**
   - ห้ามให้บอท Crash จนโปรแกรมปิด ให้ใช้ `try-except` แล้วบันทึก Error ลง Log แทน
   - ถ้าอัปโหลดไม่ผ่าน ให้ข้ามไปเพจถัดไปทันที อย่าวนลูปตาย (Infinite Loop)

4. **Dashboard Awareness:**
   - ถ้ามีการเพิ่มฟีเจอร์ใหม่ใน `bot.py` อย่าลืมไปอัปเดตปุ่มกดใน `dashboard.py` ด้วย

## 🎯 User Context (เจ้าของโปรเจกต์)
- ผู้ใช้เข้าใจ Logic แต่ไม่แม่น Syntax (เขียน Python ไม่คล่อง)
- ชอบคำอธิบายภาษาไทยง่ายๆ ไม่ต้องใช้ศัพท์เทคนิคเยอะ
- เน้น "ผลลัพธ์" (โพสต์ติด/ไม่ติด) มากกว่าความสวยงามของโค้ด