# 🚀 วิธีรันบอทบนคอมใหม่ (Copy-Paste Guide)

## ขั้นตอนที่ 1: ติดตั้ง Python และ Chrome

1. ดาวน์โหลด Python 3.10+ จาก https://python.org
2. ติดตั้ง Google Chrome เวอร์ชันล่าสุด

---

## ขั้นตอนที่ 2: สร้าง Virtual Environment

```powershell
# เปิด PowerShell ในโฟลเดอร์โปรเจกต์
cd C:\path\to\MyBot

# สร้าง venv
python -m venv venv

# เปิดใช้งาน venv
.\venv\Scripts\Activate

# ติดตั้ง dependencies
pip install -r requirements.txt
```

---

## ขั้นตอนที่ 3: ตั้งค่า .env

```powershell
# คัดลอก template
copy .env.example .env

# แก้ไขไฟล์ .env ใส่ค่าจริง
notepad .env
```

ใส่ค่า Telegram token และ chat_id:
```
TELEGRAM_TOKEN=your_real_token
TELEGRAM_CHAT_ID=your_real_chat_id
```

---

## ขั้นตอนที่ 4: Login Facebook ครั้งแรก

```powershell
python login_fix.py
```

1. Chrome จะเปิดขึ้นมา
2. ล็อกอิน Facebook ด้วยมือ
3. กด "จดจำฉัน" / "Remember me"
4. กลับมากด Enter ที่หน้าต่าง PowerShell

---

## ขั้นตอนที่ 5: ทดสอบ Dry-Run

```powershell
# ดูตารางก่อน (ไม่โพสต์จริง)
python bot.py --quota --dry-run
```

ตรวจสอบว่าตารางสมเหตุสมผล

---

## ขั้นตอนที่ 6: ทดสอบรันจริง 1 งาน

```powershell
# ทดสอบโพสต์ 1 ครั้ง เพจแรก
python bot.py --quota --max-tasks 1 --pages 0
```

---

## ขั้นตอนที่ 7: รันจริงแบบเต็ม

```powershell
# รันตามตาราง Daily Quota
python bot.py --quota
```

---

## คำสั่งที่ใช้บ่อย

| คำสั่ง | คำอธิบาย |
|--------|----------|
| `python bot.py --quota --dry-run` | ดูตาราง ไม่โพสต์ |
| `python bot.py --quota` | รันตามตาราง |
| `python bot.py --quota --max-tasks 1` | ทดสอบ 1 งาน |
| `python bot.py --now --pages 0` | โพสต์ทันที เพจ 0 |
| `python login_fix.py` | Login ใหม่ |
| `python dashboard.py` | เปิด GUI Dashboard |

---

## (Optional) เปิด Dashboard GUI

ถ้าต้องการใช้หน้า Dashboard แทน command line:

```powershell
python dashboard.py
```

จะเปิดหน้าต่าง GUI สำหรับ:
- เลือกเพจที่จะโพสต์
- สร้างตารางเวลาอัตโนมัติ
- กดปุ่ม Run Now หรือ Schedule Mode

---

## Troubleshooting

### ❌ Chrome ไม่เปิด
- ตรวจสอบว่า Chrome ติดตั้งแล้ว
- ลอง reinstall: `pip install --upgrade selenium`

### ❌ Login หลุด
- รัน `python login_fix.py` ใหม่
- ลบโฟลเดอร์ `bot_brain/` แล้ว login ใหม่

### ❌ Telegram ไม่ส่ง
- ตรวจสอบ `.env` ว่าใส่ค่าถูกต้อง
- ทดสอบ: `python get_telegram_id.py`

### ❌ Dashboard ไม่เปิด
- ตรวจสอบว่า customtkinter ติดตั้งแล้ว: `pip install customtkinter`

